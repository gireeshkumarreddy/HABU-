# HABU — Netlify configuration.
# The official Next.js runtime handles the App Router build output
# (declared explicitly so CLI deploys match UI-connected builds).

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
