# HABU — Netlify configuration.
# The site is a Next.js static export (next.config.ts → output: "export"),
# so it publishes the prebuilt `out/` directory as a plain static site — no
# Next.js runtime plugin required.

[build]
  command = "npm run build"
  publish = "out"
